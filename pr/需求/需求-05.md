# 支持远程运行的 MCP 服务模式

- 需求编号：`需求-05`
- 时效状态：`有效`
- 实现状态：`进行中`
- 所属阶段：`d2` 远程服务模式与配置模型
- 覆盖会话数：2

## 需求描述

- 让 dbt-mcp 可以独立作为远程服务运行，并明确 transport、host、port 与 API Key 鉴权。

## 业务目标

- 支持被上层系统远程调用。

## 需求依据

- 使用http streamming，需要可配置host和port，使用api key方式鉴权
- (venv) PS D:\vsx\github\dbt-mcp> Get-ChildItem Env:MCP_TRANSPORT, Env:FASTMCP_HOST, Env:FASTMCP_PORT Get-ChildItem: Cannot find path 'MCP_TRANSPORT' because it does not exist. Get…
- 这样，我们改成 streamable-http 模式吧，这样就不用在 claude code 项目根目录启动 dbt-mcp 了
- MCP_TRANSPORT在哪里定义的？
- (venv) PS D:\vsx\github\dbt-mcp> dbt-mcp WARNING [dbt_mcp.config.settings] Platform features have been automatically disabled due to missing DBT_HOST. INFO [dbt_mcp.mcp.server] Re…
- 0.0.1 -Port 9092 WARNING: TCP connect to (127.0.0.1 :

## 边界与非目标

- 我们应该复用fastmcp的环境变量，而不是重新定义自己的，除非fastmcp没有的部分
- 同时要执行“我们应该复用fastmcp的环境变量，而不是重新定义自己的，除非fastmcp没有的部分”

## 实现约束

- 当前未抽取到明确的实现约束。

## 已确认决策

- 当前未提取到明确拍板的决策。

## 验收标准

- 我把端口改成了 9092，然后在浏览器中访问 http://127.0.0.1:9092，显示服务器拒绝访问

## 关联问题与修复记录

- 当前没有抽取到结构化的问题与修复记录。
## 覆盖/替代关系

- 当前没有明确的覆盖或替代关系。

## 相关项目/模块

- `main.py`
- `config`
- `README.md`

## 来源会话

- `2026-02-13` `019c52ca-3d2d-7803-8b2f-11e23e24c625` 需要增加一套mcp tool：
  来源：`/raid/ai/metric/pr/codex/2026/02/13/rollout-2026-02-13T00-58-31-019c52ca-3d2d-7803-8b2f-11e23e24c625.jsonl`
- `2026-02-13` `019c5484-74a5-7212-b2ba-71448e43dbab` dbt-mcp是否必须跟dbt core运行在同一台机器上？
  来源：`/raid/ai/metric/pr/codex/2026/02/13/rollout-2026-02-13T09-01-32-019c5484-74a5-7212-b2ba-71448e43dbab.jsonl`
