[project]
name = "crewai-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12, <3.14"
dependencies = [
    "crewai>=1.8.0",
    "mcp>=1.16.0",
]

[project.optional-dependencies]
dev = ["pytest>=9.0"]
